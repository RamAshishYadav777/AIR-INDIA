import React from "react";
import { Typography } from "@mui/material";

const Analytics = () => {
  return (
    <div>
      <Typography variant="h4">Analytics</Typography>
      <Typography>Flight statistics, revenue, and user activity.</Typography>
    </div>
  );
};

export default Analytics;
